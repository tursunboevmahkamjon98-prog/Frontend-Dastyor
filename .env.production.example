# Reference only — these are consumed as Docker build ARGs (see
# docker-compose.yml and Dockerfile), not read from a .env file at
# container runtime. Fill in the real values in docker-compose.yml (or a
# root .env it reads from) before building.
NEXT_PUBLIC_API_URL=https://your-domain.com/api
